{
  "name": "be-brave-nails",
  "version": "1.0.0",
  "description": "Be Brave Nails - Nail salon website with booking system",
  "main": "dev-start.js",
  "scripts": {
    "dev": "node dev-start.js",
    "start": "node dev-start.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start-frontend": "cd frontend && npm run dev",
    "start-backend": "cd backend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nail-salon",
    "booking-system",
    "react",
    "nodejs",
    "express",
    "google-calendar"
  ],
  "author": "Be Brave Nails",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
